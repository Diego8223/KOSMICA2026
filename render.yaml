# ============================================================
#  render.yaml — Kosmica en Render
#  Sube este archivo a la raíz de tu repo GitHub
#  Render lo detecta y crea todos los servicios automáticamente
# ============================================================

databases:
  - name: kosmica-db
    databaseName: kosmica
    plan: free
    region: oregon

services:

  # ── Backend Java Spring Boot ──────────────────────────────
  - type: web
    name: kosmica-backend
    region: oregon
    plan: free
    rootDir: backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SPRING_DATASOURCE_URL   # ✅ Debe coincidir con ${SPRING_DATASOURCE_URL} en application.properties
        fromDatabase:
          name: kosmica-db
          property: connectionString
      - key: MERCADOPAGO_ACCESS_TOKEN
        sync: false   # Lo ingresas manualmente en el dashboard
      - key: MERCADOPAGO_PUBLIC_KEY
        sync: false
      - key: STORE_NAME
        value: Kosmica
      - key: STORE_EMAIL
        sync: false
      - key: STORE_URL
        sync: false   # URL del frontend una vez desplegado
      - key: SENDGRID_API_KEY
        sync: false
      - key: WOMPI_PUBLIC_KEY
        sync: false   # pub_prod_XXXX o pub_test_XXXX para pruebas
      - key: WOMPI_PRIVATE_KEY
        sync: false   # prv_prod_XXXX o prv_test_XXXX para pruebas
      - key: WOMPI_EVENTS_SECRET
        sync: false   # Secreto de eventos del webhook Wompi

  # ── Frontend React ────────────────────────────────────────
  - type: web
    name: kosmica-frontend
    region: oregon
    plan: free
    rootDir: frontend
    runtime: static
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        fromService:
          name: kosmica-backend
          type: web
          property: url   # ✅ "url" da https://kosmica-backend.onrender.com — "host" daba solo el hostname sin https
      - key: REACT_APP_ADMIN_PASSWORD
        value: Kosmica2025
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
