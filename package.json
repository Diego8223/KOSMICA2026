{
  "name": "kosmica-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "axios": "^1.6.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && node -e \"const fs=require('fs'); const ts=Date.now(); const sw=fs.readFileSync('build/sw.js','utf8').replace('__BUILD_TIMESTAMP__',ts); fs.writeFileSync('build/sw.js',sw); console.log('SW cache version: kosmica-'+ts);\"",
    "test": "react-scripts test"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version"
    ]
  },
  "proxy": "http://localhost:8080"
}
